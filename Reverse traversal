class Solution {
    public static void arrayTraversalReverse(int[] arr) {
 
        int rev=0;
        for(int i=arr.length-1;i>=0;i--)
        {
            rev=arr[i];
            System.out.print(rev+" ");
        }
        
    }
}
