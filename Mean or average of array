class Solution {
    public static int findMean(int[] arr) {
        int avg=0;
        int n=arr.length;
        int sum=0;
        for(int i=0;i<n;i++)
        {
            sum+=arr[i];
        }
        avg=sum/n;
        return avg;
    }
}
